// Server startup code
import { Meteor } from 'meteor/meteor';
// User presence tracking has been removed

// Initialize server-specific code here
Meteor.startup(() => {
  // User presence functionality has been removed
  // You can add your own user status tracking here if needed in the future
  
  // Set up any fixture data or initial database setup
  
  console.log('Server application started - All services initialized');
});
